name: CI

on:
  push:           # triggers on pushes to any branch
    branches:
      - '**'

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'   # change if you need different version

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run flake8 lint (max-line-length=90)
        # If you want config from .flake8 or setup.cfg to apply, flake8 will read it.
        run: |
          flake8 --max-line-length=90 .

      - name: Run pytest
        run: |
          pytest -q

      - name: Build Docker image
        run: |
          docker build -t my-app-image:latest .